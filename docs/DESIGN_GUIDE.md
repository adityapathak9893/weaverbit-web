# DESIGN_GUIDE.md — [PRODUCT NAME]

> **What this is:** the fourth planning document — how *this* product looks and behaves visually. It **extends** the shared brand (`BRAND.md` / `weaverbit-core`); it never contradicts it. Approved by Aditya before code. (See `PROCESS.md` §3.)
>
> Replace every `[ ... ]`. If this product needs nothing beyond the shared brand, that's a valid and good answer — say so.
>
> **Status:** ☐ Draft  ☐ Approved by Aditya on ______

---

## 1. Inherited from the brand (confirm, don't redefine)
> These come from weaverbit-core unchanged. Just confirm.

- Feeling: calm, clean, precise ☐
- Fonts: Space Grotesk (headings), Inter (body), monospace (technical bits) ☐
- Colors: soft white / near-black / muted teal ☐
- Signature: monospace for labels & technical bits ☐
- Display modes: Light, Dark, High-contrast, Dim, System-auto ☐

## 2. What this product adds or changes (extensions only)
> Anything visual this product needs that the brand doesn't already give. Example: a chart needs an extra color; an app needs a specific layout. Must not break the brand feeling.

- [ ... ] — and why it doesn't conflict with the brand: [ ... ]

## 3. The pages / screens and their layout
> For each screen, a plain description of the layout. Words are fine; no need for pixel detail here.

- [screen]: [ ... ]
- [screen]: [ ... ]

## 4. The components needed
> The reusable pieces this product needs. Mark which come from weaverbit-core (shared) and which are new to this product (and would live in its feature folder).

| Component | From core, or new here? | Notes |
|---|---|---|
| [ ... ] | [ ... ] | [ ... ] |

## 5. The one signature moment (optional)
> Does this product have a single, restrained standout moment (per the brand idea of "one bold thing, everything else quiet")? If so, describe it. Keep it to one.

[ ... ]

## 6. Motion
> Any animation? Keep it minimal and respectful (must honor "reduce motion" settings). If none, say none.

[ ... ]

## 7. Accessibility floor (confirm)
> Readable down to small screens; visible keyboard focus; good contrast in ALL modes; status never shown by color alone; reduce-motion honored.

- [ ... ]

## 8. Open questions for Aditya
- [ ... ]

---
*Approval gate: Aditya approves before code. (PROCESS.md §3)*
